# Before running the project, you must:
# cp env.template .env
# Configure per your needs
# docker compose up -d

# Git ref/tag for okx/xlayer-erigon used for local build and image tag
# Example: v0.9.2 (matches the Dockerfile default)
ERIGON_VERSION=v0.9.2

# Basic Configuration
DATADIR=/home/erigon-data


# Network Configuration
HTTP_PORT=28645
WS_PORT=28646


# Performance Configuration
MEM_LIMIT=16g

# zkEVM Mainnet essentials
ZKEVM_L1_RPC_URL=
