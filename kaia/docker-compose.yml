services:
  kaia-en:
    image: ${KAIA_IMAGE}
    container_name: kaia-en
    restart: unless-stopped
    stop_grace_period: 120s
    env_file: .env
    ports:
      - "${RPC_BIND_ADDR}:${HTTP_PORT}:8551"
      - "${RPC_BIND_ADDR}:${WS_PORT}:8552"
      - "${P2P_PORT}:32323"
      - "${P2P_SUBPORT}:32324"
      - "${RPC_BIND_ADDR}:${METRICS_PORT}:61001"
    volumes:
      - "${HOST_DATADIR:-$HOME/kaia-data}:/var/kend/data"
      - "./config:/config:ro"
      - "./entrypoint.sh:/entrypoint.sh:ro"
    entrypoint: ["/bin/sh", "/entrypoint.sh"]
    logging:
      options:
        max-size: "10m"
        max-file: "3"
